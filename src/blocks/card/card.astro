---
import { Image } from "astro:assets";
import "./card.css";
const {
    imageUrl, alt, class: className= ''
} = Astro.props
---

<div class={`card ${className}`}>
    {
        imageUrl ? <Image
        src={imageUrl}
        alt={alt || imageUrl}
        loading={"lazy"}
        width={100}
        height={100}
    /> : ''
    }
    <div class="card__content">
        <slot/>
    </div>
</div>
